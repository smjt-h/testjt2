---
kind: pipeline
name: pipeline-1

platform:
  os: linux
  arch: amd64

steps:
  - name: step-1
    image: alpine
    commands:
      - echo "Hello from pipeline-11"
  - name: step-2
    image: alpine
    commands:
      - echo "Hello from pipeline-22"
# trigger:
#   event:
#   - pull_request
#   action:
#     include:
#     - opened
#     - ready_for_review
